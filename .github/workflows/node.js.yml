# Disabled — replaced by .github/workflows/ci.yml
# Kept so GitHub does not keep the old filename active on PRs.
name: Node.js CI (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the CI workflow instead"
