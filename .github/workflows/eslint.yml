# Disabled — ESLint now runs inside .github/workflows/ci.yml
name: ESLint (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the CI workflow instead"
