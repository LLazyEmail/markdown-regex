const { REGEXP_EMPTY_OL } = require('../src/index');

describe('RegEx: REGEXP_EMPTY_OL', () => {

    test('should match the expected ...', () => {
    });
    test('', () => {
    });
    test('', () => {
    });
});