const { REGEXP_Q } = require('../src/index');

describe('RegEx: REGEXP_Q', () => {

    test('should match the expected ...', () => {
    });
    test( , () => {
    });
    test( , () => {
    });
});