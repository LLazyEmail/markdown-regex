const { REGEXP_EMPTY_UL } = require('../src/index');

describe('REGEXP_EMPTY_UL', () => {

    test('should match the expected ...', () => {
    });
    test( , () => {
    });
    test( , () => {
    });
});