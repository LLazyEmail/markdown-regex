const { REGEXP_CODE } = require('../src/index');

describe('REGEXP_CODE', () => {

    test('should match the expected ...', () => {
    });
    test( , () => {
    });
    test( , () => {
    });
});