const { REGEXP_UL_LIST } = require('../../src/index');

describe('RegEx: REGEXP_UL_LIST', () => {

    test('should match the expected ...', () => {
    });
    test('', () => {
    });
    test('', () => {
    });
});