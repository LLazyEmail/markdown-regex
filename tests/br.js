const { REGEXP_BR } = require('../src/index');

describe('RegEx: REGEXP_BR', () => {

    test('should match the expected ...', () => {
    });
    test( , () => {
    });
    test( , () => {
    });
});