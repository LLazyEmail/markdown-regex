const { REGEXP_BLOCKQUOTE } = require('../src/index');

describe('REGEXP_BLOCKQUOTE', () => {

    test('should match the expected ...', () => {
    });
    test( , () => {
    });
    test( , () => {
    });
});