const { REGEXP_DEL } = require('../../src/index');

describe('RegEx: REGEXP_DEL', () => {

    test('should match the expected ...', () => {
    });
    test('', () => {
    });
    test('', () => {
    });
});