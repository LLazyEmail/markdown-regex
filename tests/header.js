const { REGEXP_MEM } = require('../src/index');

describe('RegEx: REGEXP_MEM', () => {

    test('should match the expected ...', () => {
    });
    test('', () => {
    });
    test('', () => {
    });
});